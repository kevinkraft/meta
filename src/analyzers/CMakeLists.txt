project(meta-analyzers)

add_subdirectory(tokenizers)

#add_library(meta-analyzers)
#target_link_libraries(meta-analyzers meta-tokenizers)

#install(TARGETS meta-analyzers
#        EXPORT meta-exports
#        DESTINATION lib)
